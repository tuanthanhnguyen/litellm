services:
  - type: web
    name: openai-proxy
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 4000
    numInstances: 1
    healthCheckPath: /health/liveliness
    autoDeploy: true
